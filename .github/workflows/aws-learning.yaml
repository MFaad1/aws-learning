name: MyfirstGithubAction button

on:
  push:
    branches:
      - dev

jobs:
  deploy-to-lambda:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: Deploy to AWS Lambda
        uses: softprops/action-aws-lambda@v2
        with:
          AWS_ACCESS_KEY_ID: AKIAU3PM3TU5MBZ5C3UR
          AWS_SECRET_ACCESS_KEY: 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
          AWS_REGION: 'us-east-1'
          FUNCTION_NAME: 'myFirstlamba'
          HANDLER: './app.js'
          ZIP: '.'  